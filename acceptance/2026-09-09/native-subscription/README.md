# Native ChatGPT subscription candidate

The [current scoped matrix](CURRENT-MATRIX.md) and [archived gate review](final-qualification/GATE-REVIEW.md) identify the exact `ac4f14ee` runtime. The separately archived [kernel-storage and operating-cost followup](kernel-storage/README.md) closes the selected SQLite unavailability qualification and records three paired healthy reads. Publication remains open. The [archive manifest](final-qualification/ARCHIVE-MANIFEST.json) retains prior final-cutpoint, boundary, lifecycle and expiry evidence without changing the tested package. Overall acceptance remains open.

The API-key credit failure did not block native ChatGPT authentication. Codex 0.153.4 completed actual provider-backed protected work using its native subscription cache in the trusted parent. The child received only an ephemeral model-relay token. No account cache is distributed.

The exact cold-installed artifact in artifact.json passed a four-tool useful workflow, independent forbidden read/write denials, actual native patch/configuration-tamper refusals, seven approval stages, capability/credential revocation, in-flight revocation, kernel death/malformed response/timeout, six startup authority/dependency refusals, three evidence substitution cases, exclusive owner enforcement, and the four-stage aggregate budget. Resource observations use the independent readonly Docker resource and dispatch volumes.

Actual final-hop result substitution retained zero host-delivery ACKs. Lost result and gateway death each retained one original effect, refused automatic replay, and recovered only after explicit retained-result receipt/ACK followed by one native read. A separate unknown-outcome case passed a forged owner record rejection, valid owner-signed import, explicit receipt/ACK, and one native read with no repeated write. The failed first owner-import attempt selected a nonexistent operator CLI; it remains failed, including its meaningless forged rejection. The corrected test requires the specific signature-verification failure.

These are current bounded host cases, not blanket I01-I08 acceptance. Complete all inventory/lifecycle/publication requirements before claiming acceptance. Historical hook-only mode remains unsafe; use the protected launcher. The source hash and archive hash are distinct because evidence commits are made after artifact tests.
